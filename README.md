# docker-demo
Very basic docker demo

## Build with Docker

`docker build . -t docker-demo:latest`

## Run with Docker

`docker run -d docker-demo:latest -p 8080:80`

## Check its Running

`docker ps`